import type { GuardianDefinition, GuardianId, GuardianState } from "../types";

const stateProfile = (
  prefix: string,
  windupMs: number,
  attackMs: number,
  impactAtMs: number,
  recoveryMs: number,
): GuardianDefinition["animation"] => ({
  impactAtMs,
  states: {
    idle: { key: `${prefix}-idle`, loop: true, durationMs: 900 },
    windup: { key: `${prefix}-windup`, loop: false, durationMs: windupMs },
    attack: { key: `${prefix}-attack`, loop: false, durationMs: attackMs },
    recovery: { key: `${prefix}-recovery`, loop: false, durationMs: recoveryMs },
    disabled: { key: `${prefix}-disabled`, loop: true, durationMs: 700 },
  } satisfies Record<GuardianState, { key: string; loop: boolean; durationMs: number }>,
});

export const GUARDIANS: Record<GuardianId, GuardianDefinition> = {
  "pistol-shrimp": {
    id: "pistol-shrimp",
    name: "Camarão-Pistola",
    shortName: "Camarão",
    description: "Disparo forte em alvo único.",
    color: 0xff7043,
    accent: 0x24c8ef,
    cost: 60,
    range: 188,
    damage: 28,
    cooldownMs: 900,
    attackKind: "projectile",
    projectileSpeed: 430,
    timings: { windupMs: 180, attackMs: 180, recoveryMs: 540 },
    animation: stateProfile("shrimp", 180, 180, 55, 540),
    upgrade: {
      name: "Tiro Pressurizado",
      description: "Mais dano e atravessa um segundo inimigo.",
      cost: 55,
      damageMultiplier: 1.35,
      extraTargets: 1,
    },
  },
  jellyfish: {
    id: "jellyfish",
    name: "Água-viva",
    shortName: "Água-viva",
    description: "Descarga que desacelera.",
    color: 0xb86bff,
    accent: 0x6fe9ff,
    cost: 75,
    range: 166,
    damage: 11,
    cooldownMs: 1050,
    attackKind: "chain",
    slowFactor: 0.68,
    slowDurationMs: 1500,
    timings: { windupMs: 260, attackMs: 200, recoveryMs: 590 },
    animation: stateProfile("jellyfish", 260, 200, 80, 590),
    upgrade: {
      name: "Rede Elétrica",
      description: "A descarga salta para um segundo alvo.",
      cost: 65,
      damageMultiplier: 1.2,
      extraTargets: 1,
    },
  },
  pufferfish: {
    id: "pufferfish",
    name: "Baiacu",
    shortName: "Baiacu",
    description: "Pulso que atinge uma área.",
    color: 0xa8d85e,
    accent: 0xffe082,
    cost: 100,
    range: 126,
    damage: 18,
    cooldownMs: 1500,
    attackKind: "area",
    slowFactor: 0.8,
    slowDurationMs: 700,
    timings: { windupMs: 340, attackMs: 240, recoveryMs: 920 },
    animation: stateProfile("pufferfish", 340, 240, 100, 920),
    upgrade: {
      name: "Superinflação",
      description: "Pulso maior, mais forte e com lentidão.",
      cost: 75,
      damageMultiplier: 1.25,
      rangeMultiplier: 1.28,
      slowMultiplier: 0.8,
    },
  },
};

export const GUARDIAN_ORDER: GuardianId[] = ["pistol-shrimp", "jellyfish", "pufferfish"];
